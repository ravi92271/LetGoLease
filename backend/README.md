## Running the backend

To run the backend, you must cd into the backend in the terminal via `cd backend` and run the following command:
    `uv run uvicorn app.main:app --reload`

Open [http://localhost:8000](http://localhost:8000) with your browser to see the result. Use Ctrl + C to exit out of the servers.