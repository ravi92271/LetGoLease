## Getting Started

To run the development server, cd into the frontend folder via `cd frontend` and run the following command in your terminal:
    `npm run dev`

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result. Use Ctrl + C to exit out of the servers.